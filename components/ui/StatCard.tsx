import { Card, CardContent } from "@/components/ui/Card";
export function StatCard({ title, value, description }: { title: string; value: string; description?: string }) { return <Card><CardContent><p className="text-sm font-semibold text-[#68799f]">{title}</p><p className="mt-2 text-3xl font-extrabold text-[#0d1b57]">{value}</p>{description && <p className="mt-2 text-xs text-[#8794b1]">{description}</p>}</CardContent></Card>; }
