import { Avatar } from "@/components/ui/Avatar";
import { Badge } from "@/components/ui/Badge";
import { Button } from "@/components/ui/Button";
import { mockProfile } from "@/lib/mock-data";
export function ProfileHeader({ onSchedule }: { onSchedule: () => void }) { return <section className="overflow-hidden rounded-3xl border border-[#dfe7f8] bg-white shadow-[0_20px_50px_rgba(24,67,150,0.08)]"><div className="h-28 bg-[radial-gradient(circle_at_20%_30%,#ffb65b_0,transparent_24%),linear-gradient(120deg,#06296f,#0b4bff)] sm:h-36"/><div className="p-5 sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-start"><Avatar name={mockProfile.displayName} className="-mt-16 h-24 w-24 border-4 border-white text-3xl shadow-lg sm:-mt-20 sm:h-32 sm:w-32"/><div className="min-w-0 flex-1"><div className="flex flex-col justify-between gap-4 lg:flex-row"><div><h1 className="text-3xl font-extrabold text-[#0d1b57] sm:text-4xl">{mockProfile.displayName} <span className="text-xl text-brand-blue">✓</span></h1><p className="mt-1 font-medium text-[#68799f]">@{mockProfile.username}</p><p className="mt-2 text-lg font-semibold text-[#274477]">{mockProfile.title}</p></div><div className="flex flex-wrap gap-2"><Button onClick={onSchedule}>Agendar Peek</Button><Button href="/messages" variant="secondary">Mensagem</Button><Button variant="ghost" aria-label="Compartilhar perfil">Compartilhar</Button></div></div><div className="mt-4 flex flex-wrap gap-2">{mockProfile.specialties.map((item) => <Badge key={item}>{item}</Badge>)}</div><div className="mt-5 grid grid-cols-2 gap-3 border-t border-[#e8edf8] pt-5 sm:grid-cols-4"><Metric value={mockProfile.peekersCount} label="Peekers"/><Metric value={mockProfile.peeksCount} label="Peeks"/><Metric value={`${mockProfile.rating} ★`} label={`${mockProfile.reviewsCount} avaliações`}/><Metric value="Até 2h" label="Tempo de resposta"/></div></div></div></div></section>; }
function Metric({ value, label }: { value: string | number; label: string }) { return <div><strong className="block text-xl text-[#0d1b57]">{value}</strong><span className="text-sm text-[#68799f]">{label}</span></div>; }
