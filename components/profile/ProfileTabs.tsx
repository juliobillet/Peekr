export type ProfileTab = "Sobre" | "Agenda" | "Peeks" | "Mensagens";
export function ProfileTabs({ active, onChange }: { active: ProfileTab; onChange: (tab: ProfileTab) => void }) { const tabs: ProfileTab[] = ["Sobre", "Agenda", "Peeks", "Mensagens"]; return <div className="overflow-x-auto border-b border-[#dfe7f8]"><div className="flex min-w-max gap-1">{tabs.map(tab => <button key={tab} onClick={() => onChange(tab)} className={`min-h-12 border-b-2 px-5 text-sm font-bold transition ${active === tab ? "border-brand-blue text-brand-blue" : "border-transparent text-[#65779e] hover:text-[#0d1b57]"}`}>{tab}</button>)}</div></div>; }
