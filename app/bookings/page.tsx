import { Badge } from "@/components/ui/Badge";
import { Button } from "@/components/ui/Button";
import { Card, CardContent } from "@/components/ui/Card";
import { mockBookings } from "@/lib/mock-data";
export default function BookingsPage() { return <div className="space-y-6"><div><h1 className="text-3xl font-extrabold text-[#0d1b57]">Meus Peeks</h1><p className="mt-2 text-[#68799f]">Acompanhe suas reservas simuladas.</p></div><div className="flex gap-2 overflow-x-auto pb-1"><Badge variant="violet" className="px-4 py-2">Próximos</Badge><Badge className="px-4 py-2">Passados</Badge><Badge className="px-4 py-2">Cancelados</Badge></div><div className="grid gap-4">{mockBookings.map(booking => <Card key={`${booking.title}-${booking.date}`}><CardContent className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-extrabold text-[#0d1b57]">{booking.title}</h2><Badge variant={booking.status === "Confirmado" ? "success" : booking.status === "Cancelado" ? "danger" : "default"}>{booking.status}</Badge></div><p className="mt-2 text-[#68799f]">{booking.date} · {booking.time}</p></div><Button href={booking.active ? "/room/demo-room" : undefined} disabled={!booking.active} variant={booking.active ? "primary" : "secondary"} className="w-full sm:w-auto">Entrar no Peek</Button></CardContent></Card>)}</div></div>; }
